z=int(input('Наберите количество чисел для сортировки: '))
a=[0]*z
print()
for i in range(z):
    a[i]=int(input('Введите число : '))
print()
print('Метод Sorted: ', sorted(a))
print()
def fun(a):
    for x in range(0, len(a) - 1):
        for j in range(len(a) - 1, 0):
            if (a[j]>a[j+1]):
                a[j],a[j+1]=a[j+1],a[j]
    return a
print('Пузырьковый метод', fun(a))
